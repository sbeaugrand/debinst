#!/usr/bin/make -f

%:
	dh $@

override_dh_shlibdeps:
	dh_shlibdeps -l$(XCDIR)/$(XC)-$(XCVER)/usr/lib:$(XCDIR)/$(XC)-$(XCVER)/usr/lib/$(XC):$(XCDIR)/$(XC)-$(XCVER)/usr/local/lib -Xmpclient

override_dh_installsystemd:
	dh_installsystemd --no-enable
